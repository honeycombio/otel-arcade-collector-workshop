# Template — copy to honeycomb-secret.yaml and fill in the API key, then apply.
# Do NOT commit honeycomb-secret.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: honeycomb
  namespace: otel-system
type: Opaque
stringData:
  api-key: "REPLACE_ME_WITH_YOUR_HONEYCOMB_API_KEY"
